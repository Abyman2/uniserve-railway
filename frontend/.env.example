# If your frontend and backend are deployed to different domains (recommended on Railway),
# set this so the frontend calls the backend correctly.
#
# Example:
# VITE_API_BASE_URL=https://your-backend-service.up.railway.app
VITE_API_BASE_URL=

