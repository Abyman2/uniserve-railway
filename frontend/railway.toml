[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# VITE_API_BASE_URL is passed as a Docker build-arg (see Dockerfile ARG)

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
